# Generated build artifacts
.next/
node_modules/
.husky/

# Don't lint the static public assets
public/sw.js

# Don't lint shell scripts (eslint can't parse bash)
scripts/*.sh

# Don't lint JSON config (eslint can't parse it)
vercel.json
package.json
tsconfig.json
next.config.mjs
